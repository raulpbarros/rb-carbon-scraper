"""Xpansiv/APX registry software — the fifth platform.

A platform, not a registry: one classic-ASP report module (`myrpt.asp`, the
`xxxx2` form, `rptdownload.asp`) serves every tenant on `*.apx.com`. Climate
Action Reserve is the one with an adapter today; Climate Forward is the same
module and one subclass away. See `api.py` for the contract and its traps, and
`docs/api-contract-car.md` for the measurements behind them.
"""
