"""The legacy Markit Environmental Registry public view.

A platform, not a registry: one HTML view serves 21 programmes behind a
`standardId` parameter. See `api.py` for the contract and its traps.
"""
